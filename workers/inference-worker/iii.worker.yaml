name: inference-worker
runtime:
  kind: python
  package_manager: pip
  entry: inference_worker.py
scripts:
  install: "pip install -r requirements.txt"
  start: "watchfiles 'python inference_worker.py'"
resources:
  # Adjusted hints for t3.micro (1 GiB RAM, 2 vCPU)
  memory: 1024
  cpus: 2
